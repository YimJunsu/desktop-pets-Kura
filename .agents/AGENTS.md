# Project Rules

- **Git Commit and Push Constraint**: Do NOT perform `git commit` or `git push` operations automatically under any circumstances unless explicitly and directly instructed by the user in the prompt.
